package team2.lmssystem;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class LmsSystemApplication {

    /**
     * Application bootstrap method.
     *
     * @param args command-line arguments passed to the JVM (not used currently)
     */
    public static void main(String[] args) {
        SpringApplication.run(LmsSystemApplication.class, args);
    }
}
